

public class htmlParserTEster {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*
		HTMLParser htmlParser = new HTMLParser();
	
		
		String htmlData = null;
		htmlData = DoHTTPRequest.makeHTTPGetRequest("http://webgis.nmdc.eu/knmi/is-enes/drupal-7.8/?q=Uncertainties");
		HTMLParserNode hTMLParserNode = htmlParser.parseHTMLDocument(htmlData);
		String html="";
		//html = htmlParser.printTree(htmlParser.searchTreeNodes(hTMLParserNode,"div class=\"breadcrumb\""));
		HTMLParserNode breadCrumbs=htmlParser.searchTreeNodes(hTMLParserNode,"div class=\"breadcrumb\"");
		for(int j=0;j<breadCrumbs.getNumNodes();j++){
				if(!breadCrumbs.getNode(j).content.equals("Home")){
					html+=htmlParser.printTree(breadCrumbs.getNode(j));
				}
		}
		//html += htmlParser.printTree(htmlParser.searchTreeNodes(hTMLParserNode,"h1 class=\"title\" id=\"page-title\""));
		//html += htmlParser.printTree(htmlParser.searchTreeNodes(hTMLParserNode,"div id=\"block-system-main\" class=\"block block-system\""));
		System.out.println(html);
		*/
	}

}
